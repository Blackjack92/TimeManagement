<UserControl x:Class="TimeManager.Navigation.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeManager.Navigation.Views"
             xmlns:Inf="clr-namespace:TimeManager.Infrastructure;assembly=TimeManager.Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             >

    <StackPanel Orientation="Vertical">
        <Button Content="Manage projects" Command="{Binding NavigationCommand}" CommandParameter="{x:Static Inf:ViewNames.MainViewManageProjects}" />
        <Button Content="Manage todos" Command="{Binding NavigationCommand}" CommandParameter="{x:Static Inf:ViewNames.MainViewManageTodos}" />
    </StackPanel>
    
</UserControl>
