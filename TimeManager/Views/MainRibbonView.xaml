<UserControl x:Class="TimeManager.Shell.Views.MainRibbonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeManager.Shell.Views"
             xmlns:prism="http://www.codeplex.com/prism"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Ribbon prism:UIElementExtension.MergeOrder="10" 
            x:Name="MainMenu" x:FieldModifier="protected internal"
            prism:RegionManager.RegionName="MainMenu">
        
        <RibbonTab Header="General"
             prism:UIElementExtension.MergeKey="GeneralTab"
             prism:UIElementExtension.MergeOrder="10">
            <RibbonGroup Header="Group 1">
                <RibbonButton Label="Group1.Button1 (Main)" />
            </RibbonGroup>
            <RibbonGroup Header="Group 2"
                 prism:UIElementExtension.MergeKey="GeneralTabGroup2">
                <RibbonButton Label="Group2.Button1 (Main)" />
                <RibbonButton Label="Group2.Button2 (Main)" />
            </RibbonGroup>
        </RibbonTab>
        <RibbonTab Header="Last Tab"
             prism:UIElementExtension.MergeKey="LastTab"
             prism:UIElementExtension.MergeOrder="100000">
        </RibbonTab>
    </Ribbon>
</UserControl>
