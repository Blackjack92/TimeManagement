<UserControl x:Class="TimeManager.Shell.Views.RibbonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeManager.Shell.Views"
             xmlns:inf="clr-namespace:TimeManager.Infrastructure;assembly=TimeManager.Infrastructure"
             xmlns:ribbon="clr-namespace:Prism.RibbonRegionAdapter;assembly=TimeManager.Infrastructure"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Ribbon ribbon:UIElementExtension.MergeOrder="10"
        x:Name="MainMenu" x:FieldModifier="protected internal"
        prism:RegionManager.RegionName="{x:Static inf:RegionNames.RibbonRegion}">
        <RibbonTab Header="General"
             ribbon:UIElementExtension.MergeKey="GeneralTab"
             ribbon:UIElementExtension.MergeOrder="10">
            <RibbonGroup Header="Group 1">
                <RibbonButton Label="Group1.Button1 (Main)" />
            </RibbonGroup>
            <RibbonGroup Header="Group 2"
                 ribbon:UIElementExtension.MergeKey="GeneralTabGroup2">
                <RibbonButton Label="Group2.Button1 (Main)" />
                <RibbonButton Label="Group2.Button2 (Main)" />
            </RibbonGroup>
        </RibbonTab>
        <RibbonTab Header="Last Tab"
             ribbon:UIElementExtension.MergeKey="LastTab"
             ribbon:UIElementExtension.MergeOrder="1000">
        </RibbonTab>
    </Ribbon>

</UserControl>
